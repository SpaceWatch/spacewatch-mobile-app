enum Routes {
  BASE = "/",

  ALERTS_BASE = "/alerts",
  LIST_ALL_ALERTS = "/alerts/all",
  ALERTS_PAGE = "/alerts/page",

  WALLET_BASE = "/wallet",
  WALLET_RECOVER = "/wallet/recover",
}

export default Routes;
