enum Routes {
  BASE = "/",

  ALERTS_BASE = "/alerts",
  LIST_ALL_ALERTS = "/alerts/all",

  WALLET_BASE = "/wallet",
  WALLET_RECOVER = "/wallet/recover",
  WALLET_NEW = "/wallet/new",
}

export default Routes;
